import { answeredQuestion } from 'ask-me/helpers/answered-question';
import { module, test } from 'qunit';

module('Unit | Helper | answered question');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answeredQuestion([42]);
  assert.ok(result);
});
